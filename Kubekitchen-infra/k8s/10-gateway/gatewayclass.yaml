apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: kgateway-class
  # NO namespace - GatewayClass is cluster-scoped
spec:
  controllerName: kgateway.dev/kgateway
  description: "KGATEWAY for KubeKitchen"